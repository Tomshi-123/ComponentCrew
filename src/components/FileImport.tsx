import type { TableData } from "../types/Types";

type FileImportProps = {
  onDataLoaded: (data: TableData) => void;
};

export default function Import({ onDataLoaded }: FileImportProps) {
  // Hanterar filuppladdning och parsing

  return <div></div>;
}
